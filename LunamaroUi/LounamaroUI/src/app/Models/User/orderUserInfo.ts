export interface OrderInfo {
  temporaryKey:string;
  phoneNumber: string;
  deliveryStreetAddress: string;
  city: string;
  state: string;
  postalCode: number;
  name: string;
IsPayOnDelivery: boolean;
}
